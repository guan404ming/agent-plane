[project]
name = "agent-plane"
version = "0.1.0"
description = "Claude workflow automation"
requires-python = ">=3.12"
dependencies = ["apscheduler>=3.11.2", "jinja2>=3.1.6"]

[dependency-groups]
dev = ["ruff>=0.14.10"]
